; ===========================================================================
; NASM v2.14.02 - NETWIDE ASSEMBLER FOR TRDOS 386 - Erdogan Tan - 18/09/2026 
; Disassembled from "string.c" (Dissassembler: IDA)
; NASM Syntax: Erdogan Tan
; Last Update: 19/09/2026
; ===========================================================================
; string.asm
; ----------
; This file will be included in the "nasm386.asm" file
; Inclusion order: 6

; target: NASM.PRG (flat binary)
; main assembly file: nasm386.asm
; library section: libnasm.asm
; TRDOS 386 functions: system.asm 
; data section: data.asm
; bss section: bss.asm
; launcher: crt0.asm

;
; +-------------------------------------------------------------------------+
; |   This file has been generated by The Interactive Disassembler (IDA)    |
; |           Copyright (c) 2018 Hex-Rays, <support@hex-rays.com>           |
; |                            Freeware version                             |
; +-------------------------------------------------------------------------+
;

; File Name   : C:\Users\90542\OneDrive\Masaüstü\nasm_214_c_o\string.o
; Format      : ELF for Intel 386 (Relocatable)
;
; Source File : 'string.c'

; ===========================================================================
; CODE
; ===========================================================================

; section .text

; ---------------------------------------------------------------------------
; Function: tolower_init
;
; Description: Prepare a table of tolower() results. 
;              This avoids function calls on some platforms.
;
; C Declaration: void tolower_init(void)
;
; Input (Stack): none
;
; Output: none
;
; Modified Registers: EAX, ECX, (EDX)
; ---------------------------------------------------------------------------

%assign tlti   -8
%assign i      -4

tolower_init:
                push    ebp
                mov     ebp, esp
                sub     esp, 8
                nop
                mov     eax, 0          ; int i;
                mov     [ebp+i], eax    ; for (i = 0; i < 256; i++)

tli_1:                                  ; ...
                mov     eax, [ebp+i]    ; (next) i < 256
                cmp     eax, 256
                jge     tli_4
                jmp     tli_3
; ---------------------------------------------------------------------------

tli_2:                                  ; ...
                mov     eax, [ebp+i]    ; (next) i++
                mov     ecx, eax
                inc     eax
                mov     [ebp+i], eax
                jmp     short tli_1
; ---------------------------------------------------------------------------

tli_3:                                  ; ...
                mov     eax, [ebp+i]
                mov     ecx, nasm_tolower_tab
                add     ecx, eax
                mov     eax, [ebp+i]
                push    eax
                mov     [ebp+tlti], ecx ; unsigned char nasm_tolower_tab[256];
                call    tolower
                add     esp, 4
                mov     ecx, [ebp+tlti]
                mov     [ecx], al       ; nasm_tolower_tab[i] = tolower(i);
                jmp     short tli_2
; ---------------------------------------------------------------------------

tli_4:                                  ; ...
                leave
                retn


; =============== S U B R O U T I N E =======================================

; ---------------------------------------------------------------------------
; Function: nasm_stricmp
;
; Description: String comparison
;
; C Declaration: int nasm_stricmp(const char *s1, const char *s2)
;
; Input (Stack): [EBP+8]  = *s1 (string 1)
;                [EBP+12] = *s2 (string 2)
;
; Output (EAX): Comparison result (0 = equal)
;
; Modified Registers: EAX, ECX
; ---------------------------------------------------------------------------

%assign d      -8
%assign c2     -2
%assign c1     -1
%assign s1      8
%assign s2      0Ch

nasm_stricmp:
                push    ebp
                mov     ebp, esp
                sub     esp, 8
                nop

nsc_1:                                  ; ...
                mov     eax, [ebp+s1]   ; while (1) {
                mov     ecx, eax
                inc     eax
                mov     [ebp+s1], eax
                movsx   eax, byte [ecx]
                and     eax, 0FFh
                mov     ecx, nasm_tolower_tab
                add     ecx, eax
                movzx   eax, byte [ecx]
                mov     [ebp+c1], al    ; c1 = nasm_tolower(*s1++);
                mov     eax, [ebp+s2]
                mov     ecx, eax
                inc     eax
                mov     [ebp+s2], eax
                movsx   eax, byte [ecx]
                and     eax, 0FFh
                mov     ecx, nasm_tolower_tab
                add     ecx, eax
                movzx   eax, byte [ecx]
                mov     [ebp+c2], al    ; c2 = nasm_tolower(*s2++);
                movzx   eax, [ebp+c1]   ; unsigned char c1, c2;
                movzx   ecx, [ebp+c2]
                sub     eax, ecx
                mov     [ebp+d], eax    ; d = c1-c2;
                mov     eax, [ebp+d]    ; int d;
                cmp     eax, 0          ; if (d)
                jz      nsc_2
                mov     eax, [ebp+d]    ; return d;
                jmp     nsc_6
; ---------------------------------------------------------------------------

nsc_2:                                  ; ...
                movzx   eax, [ebp+c1]
                cmp     eax, 0          ; if (!c1)
                jz      nsc_3           ; break;
                jmp     nsc_4
; ---------------------------------------------------------------------------

nsc_3:                                  ; ...
                jmp     nsc_5
; ---------------------------------------------------------------------------

nsc_4:                                  ; ...
                jmp     short nsc_1
; ---------------------------------------------------------------------------

nsc_5:                                  ; ...
                mov     eax, 0          ; return 0;

nsc_6:                                  ; ...
                leave
                retn


; =============== S U B R O U T I N E =======================================

; ---------------------------------------------------------------------------
; Function: nasm_strnicmp
;
; Description: String comparison (with byte count limit)
;
; C Declaration: int nasm_strnicmp(const char *s1, const char *s2, size_t n)
;
; Input (Stack): [EBP+8]  = *s1 (string 1)
;                [EBP+12] = *s2 (string 2)
;                [EBP+16] = n (byte count limit)
;
; Output (EAX): Comparison result (0 = equal)
;
; Modified Registers: EAX, ECX
; ---------------------------------------------------------------------------

%assign d      -8
%assign c2     -2
%assign c1     -1
%assign s1      8
%assign s2      0Ch
%assign n       10h

nasm_strnicmp:
                push    ebp
                mov     ebp, esp
                sub     esp, 8
                nop

nsnc_1:                                 ; ...
                mov     eax, [ebp+n]    ; while (n--) {
                mov     ecx, eax
                add     eax, 0FFFFFFFFh ; -1
                mov     [ebp+n], eax
                cmp     ecx, 0
                jz      nsnc_5
                mov     eax, [ebp+s1]
                mov     ecx, eax
                inc     eax
                mov     [ebp+s1], eax
                movsx   eax, byte [ecx]
                and     eax, 0FFh
                mov     ecx, nasm_tolower_tab
                add     ecx, eax
                movzx   eax, byte [ecx]
                mov     [ebp+c1], al    ; c1 = nasm_tolower(*s1++);
                mov     eax, [ebp+s2]
                mov     ecx, eax
                inc     eax
                mov     [ebp+s2], eax
                movsx   eax, byte [ecx]
                and     eax, 0FFh
                mov     ecx, nasm_tolower_tab
                add     ecx, eax
                movzx   eax, byte [ecx]
                mov     [ebp+c2], al    ; c2 = nasm_tolower(*s2++);
                movzx   eax, [ebp+c1]   ; unsigned char c1, c2;
                movzx   ecx, [ebp+c2]
                sub     eax, ecx
                mov     [ebp+d], eax    ; d = c1-c2;
                mov     eax, [ebp+d]    ; int d;
                cmp     eax, 0          ; if (d)
                jz      nsnc_2
                mov     eax, [ebp+d]    ; return d;
                jmp     nsnc_6
; ---------------------------------------------------------------------------

nsnc_2:                                 ; ...
                movzx   eax, [ebp+c1]
                cmp     eax, 0          ; if (!c1)
                jz      nsnc_3          ; break;
                jmp     nsnc_4
; ---------------------------------------------------------------------------

nsnc_3:                                 ; ...
                jmp     nsnc_5
; ---------------------------------------------------------------------------

nsnc_4:                                 ; ...
                jmp     nsnc_1
; ---------------------------------------------------------------------------

nsnc_5:                                 ; ...
                mov     eax, 0          ; return 0;

nsnc_6:                                 ; ...
                leave
                retn


; =============== S U B R O U T I N E =======================================

; ---------------------------------------------------------------------------
; Function: nasm_memicmp
;
; Description: String comparison (with constant length)
;
; C Declaration: int nasm_memicmp(const char *s1, const char *s2, size_t n)
;
; Input (Stack): [EBP+8]  = *s1 (string 1)
;                [EBP+12] = *s2 (string 2)
;                [EBP+16] = n (comparison length, number of bytes)
;
; Output (EAX): Comparison result (0 = equal)
;
; Modified Registers: EAX, ECX
; ---------------------------------------------------------------------------

%assign d      -8
%assign c2     -2
%assign c1     -1
%assign s1      8
%assign s2      0Ch
%assign n       10h

nasm_memicmp:
                push    ebp
                mov     ebp, esp
                sub     esp, 8
                nop

nmc_1:                                  ; ...
                mov     eax, [ebp+n]    ; while (n--) {
                mov     ecx, eax
                add     eax, 0FFFFFFFFh ; -1
                mov     [ebp+n], eax
                cmp     ecx, 0
                jz      nmc_3
                mov     eax, [ebp+s1]
                mov     ecx, eax
                inc     eax
                mov     [ebp+s1], eax
                movsx   eax, byte [ecx]
                and     eax, 0FFh
                mov     ecx, nasm_tolower_tab
                add     ecx, eax
                movzx   eax, byte [ecx]
                mov     [ebp+c1], al    ; c1 = nasm_tolower(*s1++);
                mov     eax, [ebp+s2]
                mov     ecx, eax
                inc     eax
                mov     [ebp+s2], eax
                movsx   eax, byte [ecx]
                and     eax, 0FFh
                mov     ecx, nasm_tolower_tab
                add     ecx, eax
                movzx   eax, byte [ecx]
                mov     [ebp+c2], al    ; c2 = nasm_tolower(*s2++);
                movzx   eax, [ebp+c1]   ; unsigned char c1, c2;
                movzx   ecx, [ebp+c2]
                sub     eax, ecx
                mov     [ebp+d], eax
                mov     eax, [ebp+d]    ; int d;
                cmp     eax, 0          ; if (d)
                jz      nmc_2
                mov     eax, [ebp+d]    ; return d;
                jmp     nmc_4
; ---------------------------------------------------------------------------

nmc_2:                                  ; ...
                jmp     short nmc_1
; ---------------------------------------------------------------------------

nmc_3:                                  ; ...
                mov     eax, 0          ; return 0;

nmc_4:                                  ; ...
                leave
                retn


; =============== S U B R O U T I N E =======================================

; ---------------------------------------------------------------------------
; Function: nasm_strsep
;
; Description: String separation
;
; C Declaration: char *nasm_strsep(char **stringp, const char *delim)
;
; Input (Stack): [EBP+8] = **stringp (pointer to string address)
;                [EBP+12] = delim (delimiter character)
;
; Output (EAX): string address (asciiz string's start address)
;
; Modified Registers: EAX, ECX, (EDX)
; ---------------------------------------------------------------------------

%assign e       -8
%assign s       -4
%assign stringp  8
%assign delim    0Ch

nasm_strsep:
                push    ebp
                mov     ebp, esp
                sub     esp, 8
                nop
                mov     eax, [ebp+stringp]
                mov     ecx, [eax]
                mov     [ebp+s], ecx    ; char *s = *stringp;
                mov     eax, [ebp+s]
                cmp     eax, 0          ; if (!s)
                jz      ns_1
                jmp     ns_2
; ---------------------------------------------------------------------------

ns_1:                                   ; ...
                mov     eax, 0          ; return NULL;
                jmp     ns_4
; ---------------------------------------------------------------------------

ns_2:                                   ; ...
                mov     eax, [ebp+delim]
                push    eax
                mov     eax, [ebp+s]
                push    eax
                call    strpbrk
                add     esp, 8
                mov     [ebp+e], eax    ; e = strpbrk(s, delim);
                mov     eax, [ebp+e]    ; char *e;
                cmp     eax, 0          ; if (e)
                jz      ns_3
                mov     eax, [ebp+e]
                mov     ecx, eax
                inc     eax
                mov     [ebp+e], eax    ; *e++ = '\0';
                mov     eax, 0
                mov     [ecx], al

ns_3:                                   ; ...
                mov     eax, [ebp+stringp]
                mov     ecx, [ebp+e]
                mov     [eax], ecx      ; *stringp = e;
                mov     eax, [ebp+s]    ; return s;

ns_4:                                   ; ...
                leave
                retn


; =============== S U B R O U T I N E =======================================

; ---------------------------------------------------------------------------
; Function: nasm_skip_spaces
;
; Description: Skip leading spaces
;
; C Declaration: char *nasm_skip_spaces(const char *p)
;
; Input (Stack): [EBP+8] = *p (string addr) -string may have leading spaces-
;
; Output (EAX): beginning address of the string
;
; Modified Registers: EAX, ECX, (EDX)
; ---------------------------------------------------------------------------

%assign p	8

nasm_skip_spaces:
                push    ebp
                mov     ebp, esp
                sub     esp, 0
                nop
                mov     eax, [ebp+p]    ; if (p)
                cmp     eax, 0
                jz      nss_2

nss_1:                                  ; ...
                mov     eax, [ebp+p]    ; while (*p && nasm_isspace(*p))
                movsx   ecx, byte [eax]
                cmp     ecx, 0
                jz      nss_2
                mov     eax, [ebp+p]
                movsx   ecx, byte [eax]
                and     ecx, 0FFh
                push    ecx
                call    isspace
                add     esp, 4
                cmp     eax, 0
                jz      nss_2
                mov     eax, [ebp+p]    ; p++;
                mov     ecx, eax
                inc     eax
                mov     [ebp+p], eax
                jmp     short nss_1
; ---------------------------------------------------------------------------

nss_2:                                  ; ...
                mov     eax, [ebp+p]    ; return (char *)p;
                leave
                retn

; =============== S U B R O U T I N E =======================================

; ---------------------------------------------------------------------------
; Function: nasm_skip_word
;
; Description: Skip leading non-spaces
;
; C Declaration: char *nasm_skip_word(const char *p)
;
; Input (Stack): [EBP+8] = *p (string addr) -string may have leading spaces-
;
; Output (EAX): address of the space (between words)
;
; Modified Registers: EAX, ECX, (EDX)
; ---------------------------------------------------------------------------

%assign p	8

nasm_skip_word:
                push    ebp
                mov     ebp, esp
                sub     esp, 0
                nop
                mov     eax, [ebp+p]    ; if (p)
                cmp     eax, 0
                jz      nsw_3

nsw_1:                                  ; ...
                mov     eax, [ebp+p]    ; while (*p && !nasm_isspace(*p))
                movsx   ecx, byte [eax]
                cmp     ecx, 0
                jz      nsw_3
                mov     eax, [ebp+p]
                movsx   ecx, byte [eax]
                and     ecx, 0FFh
                push    ecx
                call    isspace
                add     esp, 4
                cmp     eax, 0
                jz      nsw_2
                jmp     nsw_3
; ---------------------------------------------------------------------------

nsw_2:                                  ; ...
                mov     eax, [ebp+p]    ; p++;
                mov     ecx, eax
                inc     eax
                mov     [ebp+p], eax
                jmp     short nsw_1
; ---------------------------------------------------------------------------

nsw_3:                                  ; ...
                mov     eax, [ebp+p]    ; return (char *)p;
                leave
                retn


; =============== S U B R O U T I N E =======================================

; ---------------------------------------------------------------------------
; Function: nasm_zap_spaces_fwd
;
; Description: Zap leading spaces with zero
;
; C Declaration: char *nasm_zap_spaces_fwd(char *p)
;
; Input (Stack): [EBP+8] = *p (string addr) -string may have leading spaces-
;
; Output (EAX): beginning address of the string
;
; Modified Registers: EAX, ECX, (EDX)
; ---------------------------------------------------------------------------

%assign p	8

nasm_zap_spaces_fwd:
                push    ebp
                mov     ebp, esp
                sub     esp, 0
                nop
                mov     eax, [ebp+p]    ; if (p)
                cmp     eax, 0
                jz      nzsf_2

nzsf_1:                                 ; ...
                mov     eax, [ebp+p]    ; while (*p && nasm_isspace(*p))
                movsx   ecx, byte [eax]
                cmp     ecx, 0
                jz      nzsf_2
                mov     eax, [ebp+p]
                movsx   ecx, byte [eax]
                and     ecx, 0FFh
                push    ecx
                call    isspace
                add     esp, 4
                cmp     eax, 0
                jz      nzsf_2
                mov     eax, [ebp+p]    ; *p++ = 0x0;
                mov     ecx, eax
                inc     eax
                mov     [ebp+p], eax
                mov     eax, 0
                mov     [ecx], al
                jmp     short nzsf_1
; ---------------------------------------------------------------------------

nzsf_2:                                 ; ...
                mov     eax, [ebp+p]    ; return p;
                leave
                retn


; =============== S U B R O U T I N E =======================================

; ---------------------------------------------------------------------------
; Function: nasm_zap_spaces_rev
;
; Description: Zap spaces with zero in reverse order
;
; C Declaration: char *nasm_zap_spaces_rev(char *p)
;
; Input (Stack): [EBP+8] = *p (string addr) -string may have leading spaces-
;
; Output (EAX): beginning (or the last char) address of the string
;
; Modified Registers: EAX, ECX, (EDX)
; ---------------------------------------------------------------------------

%assign p	8

nasm_zap_spaces_rev:
                push    ebp
                mov     ebp, esp
                sub     esp, 0
                nop
                mov     eax, [ebp+p]    ; if (p)
                cmp     eax, 0
                jz      nzsr_2

nzsr_1:                                 ; ...
                mov     eax, [ebp+p]    ; while (*p && nasm_isspace(*p))
                movsx   ecx, byte [eax]
                cmp     ecx, 0
                jz      nzsr_2
                mov     eax, [ebp+p]
                movsx   ecx, byte [eax]
                and     ecx, 0FFh
                push    ecx
                call    isspace
                add     esp, 4
                cmp     eax, 0
                jz      nzsr_2
                mov     eax, [ebp+p]    ; *p-- = 0x0;
                mov     ecx, eax
                add     eax, 0FFFFFFFFh ; -1
                mov     [ebp+p], eax
                mov     eax, 0
                mov     [ecx], al
                jmp     short nzsr_1
; ---------------------------------------------------------------------------

nzsr_2:                                 ; ...
                mov     eax, [ebp+p]    ; return p;
                leave
                retn


; =============== S U B R O U T I N E =======================================

; ---------------------------------------------------------------------------
; Function: nasm_trim_spaces
;
; Description: Zap leading and trailing spaces
;
; C Declaration: char *nasm_trim_spaces(char *p)
;
; Input (Stack): [EBP+8] = *p (string addr) -string may have leading spaces-
;
; Output (EAX): beginning address of the string
;
; Modified Registers: EAX, (ECX), (EDX)
; ---------------------------------------------------------------------------

%assign p	8

nasm_trim_spaces:
                push    ebp
                mov     ebp, esp
                sub     esp, 0
                nop
                mov     eax, [ebp+p]
                push    eax
                call    nasm_zap_spaces_fwd
                add     esp, 4
                mov     [ebp+p], eax    ; p = nasm_zap_spaces_fwd(p);
                mov     eax, [ebp+p]
                push    eax
                call    nasm_skip_word
                add     esp, 4
                push    eax
                call    nasm_zap_spaces_fwd ; nasm_zap_spaces_fwd(nasm_skip_word(p));
                add     esp, 4
                mov     eax, [ebp+p]    ; return p;
                leave
                retn

; =============== S U B R O U T I N E =======================================

; ---------------------------------------------------------------------------
; Function: nasm_get_word
;
; Description:
;        Return the word extracted from a stream or NULL if nothing left
;
; C Declaration: char *nasm_get_word(char *p, char **tail)
;
; Input (Stack): [EBP+8]  = *p
;                [EBP+12] = **tail
;
; Output (EAX): string address
;
; Modified Registers: EAX, ECX, (EDX)
; ---------------------------------------------------------------------------

%assign next   -8
%assign word   -4
%assign p       8
%assign tail    0Ch

nasm_get_word:
                push    ebp
                mov     ebp, esp
                sub     esp, 8
                nop
                mov     eax, [ebp+p]
                push    eax
                call    nasm_skip_spaces
                add     esp, 4
                mov     [ebp+word], eax ; char *word = nasm_skip_spaces(p);
                mov     eax, [ebp+word]
                push    eax
                call    nasm_skip_word
                add     esp, 4
                mov     [ebp+next], eax ; char *next = nasm_skip_word(word);
                mov     eax, [ebp+word]
                cmp     eax, 0
                jz      ngw_2
                mov     eax, [ebp+word] ; if (word && *word) {
                movsx   ecx, byte [eax]
                cmp     ecx, 0
                jz      ngw_2
                mov     eax, [ebp+next] ; if (*next)
                movsx   ecx, byte [eax]
                cmp     ecx, 0
                jz      ngw_1
                mov     eax, [ebp+next]
                mov     ecx, eax
                inc     eax
                mov     [ebp+next], eax
                mov     eax, 0
                mov     [ecx], al       ; *next++ = '\0';

ngw_1:                                  ; ...
                jmp     ngw_3
; ---------------------------------------------------------------------------

ngw_2:                                  ; ...
                mov     eax, 0          ;  } else
                mov     [ebp+next], eax ; word = next = NULL;
                mov     [ebp+word], eax

ngw_3:                                  ; ...
                mov     eax, [ebp+tail] ; *tail = next;
                mov     ecx, [ebp+next]
                mov     [eax], ecx
                mov     eax, [ebp+word] ; return word;
                leave
                retn

; =============== S U B R O U T I N E =======================================

; ---------------------------------------------------------------------------
; Function: nasm_opt_val
;
; Description: Extract "opt=val" values from the stream and returns "opt"
;
; C Declaration: char *nasm_opt_val(char *p, char **val, char **next)
;
; Input (Stack): [EBP+8]  = *p
;                [EBP+12] = **val
;                [EBP+16] = **next
;
; Output (EAX): opt
;
; Modified Registers: EAX, ECX, EDX
; ---------------------------------------------------------------------------

%assign nxt    -8
%assign q      -4
%assign p       8
%assign val     0Ch
%assign next    10h

nasm_opt_val:
                push    ebp
                mov     ebp, esp
                sub     esp, 8
                nop
                mov     eax, [ebp+val]
                mov     ecx, [ebp+next]
                mov     edx, 0
                mov     [ecx], edx
                mov     [eax], edx      ; *val = *next = NULL;
                lea     eax, [ebp+nxt]  ; char *q, *nxt;
                push    eax
                mov     eax, [ebp+p]
                push    eax
                call    nasm_get_word
                add     esp, 8
                mov     [ebp+p], eax    ; p = nasm_get_word(p, &nxt);
                mov     eax, [ebp+p]    ; if (!p)
                cmp     eax, 0
                jz      nov_1
                jmp     nov_2
; ---------------------------------------------------------------------------

nov_1:                                  ; ...
                mov     eax, 0          ; return NULL;
                jmp     nov_8
; ---------------------------------------------------------------------------

nov_2:                                  ; ...
                mov     eax, '='
                push    eax
                mov     eax, [ebp+p]
                push    eax
                call    strchr
                add     esp, 8
                mov     [ebp+q], eax    ; q = strchr(p, '=');
                mov     eax, [ebp+q]    ; if (q) {
                cmp     eax, 0
                jz      nov_6
                mov     eax, [ebp+q]
                mov     ecx, [ebp+p]
                cmp     eax, ecx        ; if (q == p)
                jnz     nov_3
                mov     eax, 0
                mov     [ebp+p], eax    ; p = NULL;

nov_3:                                  ; ...
                mov     eax, [ebp+q]
                mov     ecx, eax
                inc     eax
                mov     [ebp+q], eax    ; *q++='\0';
                mov     eax, 0
                mov     [ecx], al
                mov     eax, [ebp+q]
                movsx   ecx, byte [eax] ; if (*q) {
                cmp     ecx, 0
                jz      nov_4
                mov     eax, [ebp+val]
                mov     ecx, [ebp+q]
                mov     [eax], ecx      ; *val = q;
                jmp     nov_5
; ---------------------------------------------------------------------------

nov_4:                                  ; ...
                mov     eax, [ebp+q]    ; } else {
                inc     eax
                lea     ecx, [ebp+nxt]
                push    ecx
                push    eax
                call    nasm_get_word
                add     esp, 8
                mov     [ebp+q], eax    ; q = nasm_get_word(q + 1, &nxt);
                mov     eax, [ebp+q]
                cmp     eax, 0          ; if (q)
                jz      nov_5
                mov     eax, [ebp+val]
                mov     ecx, [ebp+q]
                mov     [eax], ecx      ; *val = q;

nov_5:                                  ; ...
                jmp     nov_7
; ---------------------------------------------------------------------------

nov_6:                                  ; ...
                mov     eax, [ebp+nxt]  ; } else {
                push    eax
                call    nasm_skip_spaces
                add     esp, 4
                mov     [ebp+q], eax    ; q = nasm_skip_spaces(nxt);
                mov     eax, [ebp+q]
                cmp     eax, 0          ; if (q && *q == '=') {
                jz      nov_7
                mov     eax, [ebp+q]
                movsx   ecx, byte [eax]
                cmp     ecx, '='
                jnz     nov_7
                mov     eax, [ebp+q]
                inc     eax
                lea     ecx, [ebp+nxt]
                push    ecx
                push    eax
                call    nasm_get_word
                add     esp, 8
                mov     [ebp+q], eax    ; q = nasm_get_word(q + 1, &nxt);
                mov     eax, [ebp+q]
                cmp     eax, 0          ; if (q)
                jz      nov_7
                mov     eax, [ebp+val]
                mov     ecx, [ebp+q]
                mov     [eax], ecx      ; *val = q;

nov_7:                                  ; ...
                mov     eax, [ebp+next]
                mov     ecx, [ebp+nxt]
                mov     [eax], ecx      ; *next = nxt;
                mov     eax, [ebp+p]    ; return p;

nov_8:                                  ; ...
                leave
                retn

