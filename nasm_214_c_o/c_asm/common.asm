; ===========================================================================
; NASM v2.14.02 - NETWIDE ASSEMBLER FOR TRDOS 386 - Erdogan Tan - 18/09/2026 
; Disassembled from "common.c" (Dissassembler: IDA)
; NASM Syntax: Erdogan Tan
; Last Update: 18/09/2026
; ===========================================================================
; common.asm
; ----------
; This file will be included in the "nasm386.asm" file
; Inclusion order: 2

; target: NASM.PRG (flat binary)
; main assembly file: nasm386.asm
; library section: libnasm.asm
; TRDOS 386 functions: system.asm 
; data section: data.asm
; bss section: bss.asm
; launcher: crt0.asm

;
; +-------------------------------------------------------------------------+
; |   This file has been generated by The Interactive Disassembler (IDA)    |
; |           Copyright (c) 2018 Hex-Rays, <support@hex-rays.com>           |
; |                            Freeware version                             |
; +-------------------------------------------------------------------------+
;

; File Name   : C:\Users\90542\OneDrive\Masaüstü\nasm_214_c_o\common.o
; Format      : ELF for Intel 386 (Relocatable)
;
; Source File : 'common.c'

; ===========================================================================
; CODE
; ===========================================================================

; section .text

; =============== S U B R O U T I N E =======================================

; ---------------------------------------------------------------------------
; Function: prefix_name
;
; Description: Common list of prefix names; ideally should be auto-generated
;              from tokens.dat
;
; C Declaration: const char *prefix_name(int token)
;
; Input (Stack): [EBP+8] = token
;
; Output (EAX): prefix name address
;
; Modified Registers: EAX, ECX
; ---------------------------------------------------------------------------

prefix          equ -4
token           equ  8

prefix_name:
                push    ebp
                mov     ebp, esp
                sub     esp, 4     ; static const char *prefix_names[] = {
                                   ; "a16", "a32", "a64", "asp", "lock", "o16", "o32", "o64", "osp",
                                   ; "rep", "repe", "repne", "repnz", "repz", "times", "wait",
                                   ; "xacquire", "xrelease", "bnd" };
                nop
                mov     eax, [ebp+token]
                sub     eax, 0F1h       ; PREFIX_ENUM_START;
                mov     [ebp+prefix], eax ; unsigned int prefix = token-PREFIX_ENUM_START;
                mov     eax, [ebp+prefix] ; if (prefix >= ARRAY_SIZE(prefix_names))
                cmp     eax, 19
                jb      pn_1
                mov     eax, 0
                jmp     pn_2
; ---------------------------------------------------------------------------

pn_1:                                   ; ...
                mov     eax, [ebp+prefix]
                shl     eax, 2
                mov     ecx, prefix_names
                add     ecx, eax
                mov     eax, [ecx]      ; return prefix_names[prefix];

pn_2:                                   ; ...
                leave
                retn
